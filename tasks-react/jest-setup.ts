import '@testing-library/jest-dom';
// import 'cross-fetch/polyfill';
// import type {GlobalWindow} from '../types/global';
// import {setupMockBackend} from './mockBackend';

beforeEach(() => {
  // setupMockBackend();
});
